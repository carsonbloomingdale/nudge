import { GoalsContent } from "../components/books/GoalsContent";

/** Full-page route — primary path on mobile (bottom tab); desktop can use home library + takeover too. */
export default function GoalsPage() {
  return <GoalsContent />;
}
