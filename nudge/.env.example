# API origin (no trailing slash). Must match CORS + cookie Domain/SameSite on the server.
REACT_APP_API_BASE_URL=http://127.0.0.1:8000

# If you see CORS errors in local dev: use CRA proxy (see package.json "proxy") instead of
# calling the API directly. Uncomment the next line and restart `npm start`.
# REACT_APP_USE_SAME_ORIGIN_API=true

# Dev server (npm start only)
# HOST=0.0.0.0
# PORT=3000
